extends layout

block content
  h1= title
  if item_list.length
    ul
      each item in item_list
        li
          a(href=item.url) #{item.name}
          |  (#{item.price}$)

  else
    p There are no items.